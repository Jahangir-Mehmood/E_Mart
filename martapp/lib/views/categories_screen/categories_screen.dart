import '../../consts/consts.dart';

class CategoriesScreen extends StatelessWidget {
  const CategoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:

          // Wrap the widget that depends on observable variables with Obx
          Container(
        color: Colors.purple,
      ),
    );
  }
}
